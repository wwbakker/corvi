/** One tmux window of a change, as the navigation shows it — the browser-safe view type from
 * `@corvi/contracts/terminal`, re-exported while the server's imports migrate. */
export type { TerminalWindow } from "@corvi/contracts/terminal";
