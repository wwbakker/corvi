/** The settings page's read/write vocabulary (`@corvi/contracts/settings-view`), re-exported
 * while the server's imports migrate. */
export type { Settings, SettingsView } from "@corvi/contracts/settings-view";
