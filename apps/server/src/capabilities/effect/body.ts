/** Request-body readers (`@corvi/contracts/body`), re-exported so the app's callers keep one
 * import while integrations import the contract directly. */
export { bodyAs, bodyOf, bodyOrEmpty } from "@corvi/contracts/body";
