{
  "name": "corvi",
  "description": "An agent-ready development environment for a change: its repositories, pull requests, tickets, builds and terminals.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wwbakker/corvi.git"
  },
  "module": "apps/server/src/server.ts",
  "type": "module",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*",
      "integrations/*"
    ],
    "catalog": {
      "effect": "^3.22.2"
    }
  },
  "engines": {
    "node": ">=24"
  },
  "trustedDependencies": [
    "electron",
    "node-pty"
  ],
  "scripts": {
    "dev": "bun run build:web && node --watch apps/server/src/server.ts",
    "start": "NODE_ENV=production node apps/server/src/server.ts",
    "test": "CORVI_TEST_RUN=\"${CORVI_TEST_RUN:-$(date +%s).$$}\"; export CORVI_TEST_RUN; echo $$ > \"${TMPDIR:-/tmp}/corvi-$CORVI_TEST_RUN.pid\"; ROOT=$(mktemp -d \"${TMPDIR:-/tmp}/corvi-$CORVI_TEST_RUN-root-XXXXXX\"); export CORVI_ROOT=\"$ROOT\" CORVI_ARCHIVE_ROOT=\"$ROOT-archive\" CORVI_CONFIG=\"$ROOT/config.json\" XDG_STATE_HOME=\"$ROOT/state\"; trap 'status=$?; bun scripts/clean-test.ts --kill --prune --run=\"$CORVI_TEST_RUN\"; exit $status' EXIT; bun run build:web && bun test --timeout 30000 --parallel",
    "test:coverage": "trap 'status=$?; bun scripts/clean-test.ts --kill; exit $status' EXIT; CORVI_ROOT=\"${TMPDIR:-/tmp}/corvi-test-root\" CORVI_ARCHIVE_ROOT=\"${TMPDIR:-/tmp}/corvi-test-archive\" CORVI_CONFIG=\"${TMPDIR:-/tmp}/corvi-test-config.json\" XDG_STATE_HOME=\"${TMPDIR:-/tmp}/corvi-test-state\" bun run build:web && bun test --coverage --timeout 30000",
    "test:clean": "bun scripts/clean-test.ts",
    "typecheck": "tsc --noEmit",
    "boundaries": "bun scripts/architecture.ts",
    "outline": "bun scripts/outline.ts",
    "lint": "eslint .",
    "effect:lint": "effect-language-service diagnostics --project tsconfig.json --format text",
    "shot": "bun scripts/shot.ts",
    "icons": "bun scripts/icons.ts",
    "extension:install": "bun scripts/extension.ts install",
    "extension:uninstall": "bun scripts/extension.ts uninstall",
    "app:install": "bun run build:web && bun apps/desktop/src/install.ts install",
    "app:uninstall": "bun apps/desktop/src/install.ts uninstall",
    "app:run": "bun run build:web && bun apps/desktop/src/install.ts run",
    "app:permissions": "bun scripts/permissions.ts",
    "app:drive": "bun scripts/drive.ts",
    "app:sandbox": "bun scripts/sandbox.ts",
    "build:web": "bun apps/web/src/node/build.ts",
    "dev:web": "bun apps/web/src/node/build.ts --watch"
  },
  "dependencies": {
    "@corvi/azure-devops": "workspace:*",
    "@corvi/desktop": "workspace:*",
    "@corvi/github": "workspace:*",
    "@corvi/jira": "workspace:*",
    "@corvi/web": "workspace:*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1",
    "@effect/language-service": "^0.87.2",
    "@eslint/js": "^10.0.1",
    "@types/bun": "latest",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@types/ws": "^8.18.1",
    "eslint": "^10.10.0",
    "playwright": "^1.63.0",
    "typescript-eslint": "^8.70.0"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  }
}
